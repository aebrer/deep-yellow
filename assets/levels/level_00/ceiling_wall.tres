[gd_resource type="ShaderMaterial" load_steps=3 format=3]

; ============================================================================
; CEILING FOR WALL TOPS - Camera→player sightline cutout only
; ============================================================================
; This is used for the top face of wall meshes to cap them off.
; Uses the wall proximity shader which has:
; - Camera→player sightline cutout (tactical cam tunnel to see player)
; - NO ceiling proximity fade (that's only for ceiling tiles)
; The main ceiling tiles use a different shader with player-based proximity.
; ============================================================================

[ext_resource type="Shader" path="res://shaders/psx_wall_proximity.gdshader" id="1"]
[ext_resource type="Texture2D" path="res://assets/levels/level_00/textures/wallpaper_yellow.png" id="2"]

[resource]
shader = ExtResource("1")
shader_parameter/modulate_color = Color(1, 1, 1, 1)
shader_parameter/albedoTex = ExtResource("2")
shader_parameter/uv_scale = Vector2(1, 1)
shader_parameter/uv_offset = Vector2(0, 0)
shader_parameter/uv_pan_velocity = Vector2(0, 0)
shader_parameter/enable_proximity_fade = true
shader_parameter/sightline_fade_radius = 2.0
shader_parameter/use_dithered_fade = true
shader_parameter/player_position = Vector3(0, 0, 0)
